"""Ingestion CLI: loads documents/tables via the configured adapter, chunks + indexes (BM25 +
Chroma). TODO (M4/M6): wire up ingest.chunker, ingest.loaders, ingest.parsers, qa.retrieval.
"""
from __future__ import annotations


def main() -> None:
    print("ingest.cli: not yet implemented (M4) — see CLAUDE.md §5, §8 for the full spec.")


if __name__ == "__main__":
    main()
