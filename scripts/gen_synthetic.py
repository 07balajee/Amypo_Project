"""Generates everything under data/synthetic/ (CLAUDE.md §9). Seeded, reproducible.

TODO (M1): docs (syllabi/faq/policies), structured records (students, courses, grades, ...),
past_queries.csv, qa_benchmark.jsonl, router_prompts.jsonl, scenarios/*.csv, pricing.yaml.
"""
from __future__ import annotations

SEED = 20260101


def main() -> None:
    print("gen_synthetic: not yet implemented (M1) — see CLAUDE.md §9 for the full spec.")


if __name__ == "__main__":
    main()
